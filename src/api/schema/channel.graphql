type Channel {
  id: String
  name: String
  createdTimestamp: String
  type: String
  rawPosition: Int
  topic: String
  nsfw: Boolean
  bitrate: Int
  userLimit: Int
  parentID: String
  rateLimitPerUser: Int
  guild: Guild
}
